<ul class="navbar-nav ml-auto">
  <% if user_signed_in? %>
    <%# ログイン中はこちらを表示 %>
    <li class="nav-item dropdown">
      <a class="nav-link dropdown-toggle text-white" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
        ようこそ <%= current_user.name %> さん
      </a>
      <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
        <%= link_to " マイページ", user_path(current_user.id), class: 'dropdown-item fas fa-user' %>
        <%= link_to " プロットを書く", new_plot_path, class: 'dropdown-item fas fa-book-open' %>
        <div class="dropdown-divider"></div>    <%# 仕切り %>
        <%= link_to " ログアウト", destroy_user_session_path, class: 'dropdown-item fas fa-sign-out-alt', method: :delete  %>
      </div>
    </li>
  <% else %>
    <li class="nav-item dropdown">
      <a class="nav-link dropdown-toggle text-white" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
        ようこそ ゲスト さん
      </a>
      <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
        <%= link_to " 新規登録する", new_user_registration_path, class: 'dropdown-item  fas fa-user-plus' %>
        <%= link_to " ログインする", new_user_session_path, class: 'dropdown-item  fas fa-sign-in-alt' %>
      </div>
    </li>
  <% end %>
</ul>
