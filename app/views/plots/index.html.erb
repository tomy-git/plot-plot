<h1>Plots#index</h1>
<p>Find me in app/views/plots/index.html.erb</p>
<% @plots.each do |plot| %>
  <div class="row">
    <%= link_to plot_path(plot.id) do %>
      <%= plot.title %>
    <% end %>
  </div>
  <div class="row">
    <div class="col-10">
      <%== plot.body %>
    </div>
    <div class="col-2">
      いいね数 <%= plot.liked_users.count %>
    </div>
  </div>
<% end %>
<h2>tag list</h2>
<% @tag_list.each do |list| %>
  <%= link_to list.tag_name, tag_plots_path(tag_id: list.id) %>
  <%= "(#{list.plots.count})" %>
<% end %>
<div class="row">
  <%= link_to '新規作成', new_plot_path %>
</div>
<div class="row">
  <% @all_ranks.each do |plot| %>
    <%= plot.title %>
  <% end %>
</div>