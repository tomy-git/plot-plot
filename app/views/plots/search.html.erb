<% @tag_list.each do |list| %>
  <%= link_to list.tag_name, tag_plots_path(plot_tag_id: list.id) %>
  <%= "(#{list.plots.count})" %>
<% end %>
<br>
<%= "#{@tag.tag_name}" %>
<br>
<% @plots.each do |plot| %>
  <%# ここは<%==では動かないらしい。html_safeメソッドで出力させる %>
  <%= link_to plot.body.html_safe %>
<% end %>
<br>
<%= link_to 'home', root_path %>