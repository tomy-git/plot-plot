<%# タグのリンクをクリックしたらそのタグの記事だけがずらっと出るようにしたい %>

<% @tag_list.each do |list| %>
  <%= "(#{list.plots.count})" %>
<% end %>
<br>
<%= "#{@tag.tag_name}" %>
<br>
<% @plots.each do |plot| %>
  <%= link_to plot.title, plot_path(plot.id) %>
<% end %>
<br>
<%= link_to 'home', root_path %>
