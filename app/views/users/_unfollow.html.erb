<!-- フォロー解除ボタン ------------------------------------------------------------------>
<%#= form_for(current_user, url: relationship_path(@user), method: :delete, remote: true) do |f| %>
<%= form_with url: user_relationship_path(@user), method: :delete do |f| %>
  <%= f.submit "フォロー解除", class: "btn btn-outline-secondary" %>
<% end %>